# Workshop Notes

### What's JPA
__Java Persistence API__ **now** __Jakarta Persistence__

JPA is a standard Java specification that simplifies how Java objects (data) are saved or "persisted" into relational databases.
It's a group of rules, annotations and interfaces that maps Java objects and standardizes persistence operations.